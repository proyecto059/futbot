[project]
name = "futbot-ws"
version = "0.1.0"
description = "FutbotMX — Comunicación P2P entre robots autónomos"
requires-python = ">=3.11"
dependencies = [
    "websockets>=12.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.uv]
dev-dependencies = []
